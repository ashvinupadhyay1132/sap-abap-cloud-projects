@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: '3-Level Purchase Order Header View Entity'
@Search.searchable: true

define root view entity ZCDS_I_PO_HEADER_3L
  as select from /dmo/travel as POHeader

  /* Level 2 Composition: Purchase Order Items */
  composition [0..*] of ZCDS_I_PO_ITEM_3L as _Items

  association [0..1] to /DMO/I_Agency   as _PurchasingOrg on $projection.Purchasing_Org = _PurchasingOrg.AgencyID
  association [0..1] to /DMO/I_Customer as _Vendor        on $projection.Vendor_ID      = _Vendor.CustomerID
  association [0..1] to I_Currency       as _Currency      on $projection.Currency       = _Currency.Currency
{
  key travel_id       as Purchase_Order_ID,
      agency_id       as Purchasing_Org,
      customer_id     as Vendor_ID,
      begin_date      as Document_Date,
      end_date        as Target_Delivery_Date,
      description     as PO_Description,
      overall_status  as Order_Status,

      @Semantics.amount.currencyCode: 'Currency'
      total_price     as Total_PO_Value,
      currency_code   as Currency,

      /* Associations */
      _Items,
      _PurchasingOrg,
      _Vendor,
      _Currency
}
